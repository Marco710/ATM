<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LoginActivity" >


    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="身份证件：" />

    <EditText
        android:id="@+id/login_edit_identity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/textView1"
        android:ems="12"
        android:hint="请输入有效 的身份证件号码"
    />
    
 	<TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="密码：" 
        android:layout_below="@id/login_edit_identity"/>

    <EditText
        android:id="@+id/login_edit_logpwd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/textView2"
        android:ems="10"
        android:inputType="textPassword" 
        android:hint="请输入登录密码">

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/login_btn_ok"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/login_edit_logpwd"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="46dp"
        android:layout_toRightOf="@+id/textView1"
        android:text="登陆" 
    />

</RelativeLayout>